import React from 'react';

// This component has been removed as it is obsolete.
// API key management is now handled via environment variables
// as per the project guidelines, so this UI is no longer needed.
export const ApiKeyModal: React.FC = () => {
  return null;
};
